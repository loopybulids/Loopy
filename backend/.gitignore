# dependencies
/node_modules

# build output
/dist

# database (local SQLite dev) — never commit
*.db
*.db-journal
prisma/dev.db
prisma/dev.db-journal

# env / secrets
.env
.env*.local

# logs
*.log
npm-debug.log*

# tooling
*.tsbuildinfo
.eslintcache
/coverage

# production env copy for pasting into Render - never commit
.env.render
